[pytest]
DJANGO_SETTINGS_MODULE = career_mentor_ai.test_settings
python_files = tests.py test_*.py *_tests.py
addopts =
    --cov=users
    --cov=resumes
    --cov=chat
    --cov=nudge
    --cov=progress
    --cov=resume_processor
    --cov-report=term-missing
    --cov-config=.coveragerc
